# CCTV AI 실시간 관제 파이프라인 설계 (장우원 담당)

작성일: 2026-08-18 (최종 수정: 2026-09-14)

## 배경

Jetson 보드에 리눅스를 설치해(하드웨어는 강사님 담당) CCTV 영상을 AI가 실시간으로 분석하고,
결과를 `CCTV_ISSUE`(이상행동 이벤트) / `CCTV_VISITOR`(손님 입장·퇴장·체류) 테이블에 저장하는
파이프라인.

## 아키텍처 결정

- **Jetson(에지)**: 카메라 캡처 + YOLO 사람 탐지/추적 + **이상행동 유형(CODE) 최종 확정**까지 담당.
  LLM 추론은 하지 않음(GPU 자원상 H200에만 있음).
- **판단 주체는 Jetson이다 (중요)**: 서버 LLM(Ollama)은 텍스트만 읽을 수 있고 영상을 볼 수 없어서
  "이게 진짜 폭행인지"를 재검증할 방법이 없다. 그래서 최초 설계(서버 LLM이 code까지 재분류)는
  폐기하고, Jetson이 확정한 code를 서버가 그대로 신뢰하는 구조로 변경. 서버는 (1) code 유효성
  검증 (2) detail을 관리자용 한국어 문장(comnet)으로 다듬기, 두 가지만 한다.
- **DB 저장 방식**: Spring REST API를 거치지 않고 FastAPI가 `core/database.py`로 Oracle에 직접
  INSERT/UPDATE. PK는 Spring(JPA) 시퀀스(`SEQ_CCTV_ISSUE_NO`, `SEQ_CCTV_VISITOR_NO`)를 재사용.
- Jetson 워커 코드는 git 저장소에 넣지 않고 Jetson 로컬(`~/yolov5/`)에 둔다.

## Jetson 디바이스 환경

- 접속: `ssh nvidia@10.100.0.164` (교육장 내부망, DHCP라 IP가 바뀔 수 있음 → `hostname -I`로 확인)
- 보드: Jetson Nano (`t210ref`), L4T R32.7.1 / JetPack 4.6.x / Ubuntu 18.04.6 / CUDA 10.2
- **Python 3.6.9** — ultralytics/yolov5는 **v5.0 태그**만 호환
  - `from __future__ import annotations`(3.7+) 사용 불가
  - `dataclasses` 백포트 필요: `pip3 install dataclasses`
  - `python`은 **Python 2.7**이므로 반드시 `python3`로 실행 (2.7은 함수 어노테이션 문법 오류)
- PyTorch 1.8.0, torchvision 0.9.0, numpy 1.19.5, OpenCV 4.1.1 설치돼 있음
- RAM 3.9GB — **온디바이스 학습 불가**, 추론 전용(학습은 H200에서 하고 가중치만 이전)
- **카메라는 CSI(IMX219)** — `cv2.VideoCapture(0)`/`CAP_V4L2`로 열면 초록 단색 화면.
  `nvarguscamerasrc` GStreamer 파이프라인으로 열어야 정상 컬러.
- **`cv2.imshow`는 코어덤프로 죽음** (GTK2 빌드 + GTK3 동시 로드 충돌).
  → 표준 라이브러리 `http.server`로 MJPEG 스트림(8090)을 띄워 브라우저로 확인하는 방식으로 우회.
  VNC로 젯슨 데스크탑에 들어가 `http://localhost:8090/`으로 보는 게 가장 확실함(다른 PC에서는
  `http://<젯슨IP>:8090/`, 교육장 네트워크가 기기 간 통신을 막으면 안 될 수 있음).
- 성능: `IMG_SIZE` 640 → **416**으로 낮춰 **약 4fps → 8fps** 확보(`YOLO_IMG_SIZE` 환경변수로 조정).
  추적 안정성이 FPS에 직접 좌우되므로 이 조정의 효과가 컸다.

## Jetson 워커 구성 (`~/yolov5/`)

| 파일 | 역할 |
|---|---|
| `worker_skeleton.py` | 메인 루프(캡처→탐지→추적→판정→비동기 전송→디버그 스트리밍) |
| `anomaly_rules.py` | 01/03/04/05 룰 엔진 (사람 트랙 기반) |
| `scene_rules.py` | 02/06 룰 엔진 (프레임 전체 기반) |
| `person_detector.py` | YOLOv5s 추론 래퍼 (모델 1회 로드) |
| `incident.py` | 같은 상황 반복 전송 억제 + 에스컬레이션 |
| `visitor.py` | 손님 입·퇴장 집계 (CCTV_VISITOR) |
| `homography.py` | 카메라 좌표 → 도면 좌표 변환 |
| `make_plan.py` | 카메라 화면에서 도면 + homography.json 자동 생성 |
| `run_watch.sh` / `run_service.sh` | 실행 스크립트 2종 |

`~/yolov5`는 `git clone --branch v5.0` 한 ultralytics 저장소 + `yolov5s.pt`. `person_detector.py`가
그 저장소의 `models/`, `utils/`를 직접 import하므로 반드시 이 폴더 안에 함께 둬야 한다.
※ pscp로 올리면 실행 권한이 사라지므로 `chmod +x` 하거나 `bash run_watch.sh`로 실행.

## 이상행동 판정 방식

학습 없이 YOLO+추적 결과 위에 얹는 휴리스틱으로 확정(이미지 생성 AI로 분류기를 학습시키는 방향은
도메인 갭과 시계열 판단 문제로 기각, 나노에서 학습이 불가능한 것과도 부합).

| 코드 | 판정 방식 | 담당 |
|---|---|---|
| 01 폭행 | 두 사람 박스가 겹친 상태에서 "몸 크기 대비 움직임 세기" 반복 → 점수 누적 | anomaly_rules |
| 02 기물파손 | 기준 프레임 대비 변화가 급증한 뒤 그대로 유지되는지 | scene_rules |
| 03 쓰러짐 | 박스 가로/세로 비율 + 낮은 높이가 N초 유지 | anomaly_rules |
| 04 무단침입 | 영업시간 외 person 탐지 | anomaly_rules |
| 05 장시간체류 | 한 트랙이 화면에 N초 이상 체류 | anomaly_rules |
| 06 화재 | 불꽃색 **덩어리** 크기 + 깜빡임(flicker) | scene_rules |

### 01 폭행 — 판정 신호 자체가 틀렸던 것을 교체 (2026-09-14)

01이 안 잡힌 원인은 임계값이나 트래커가 아니라 **보고 있던 신호가 잘못된 것**이었다.
기존 룰은 `speed_px_per_sec`(박스 **중심의 이동 속도**)를 "격한 움직임"의 지표로 썼는데,
실제 폭행 영상을 프레임 단위로 분석해보니 **가해자는 제자리에 서서 팔만 휘두르고 피해자는 벽에
몰려 있어서 두 사람 박스의 중심이 거의 움직이지 않는다.** 임계값을 아무리 낮춰도 해결 불가.

실측값 (사람 키 대비 초당 변화량):

| 지표 | 중앙값 | 90퍼센타일 |
|---|---|---|
| 중심 이동 | 1.47 | 9.87 |
| 박스 형태 변화 (Δ폭 + Δ높이) | 2.63 | 7.80 |

팔을 뻗으면 박스의 **폭**이 출렁이므로 `Track.motion_intensity()`로 교체:
`(중심 이동거리 + |Δ폭| + |Δ높이|) / 시간 / 사람 키`.
**사람 키로 나누는 정규화가 핵심** — 카메라에서 멀면 같은 동작도 픽셀 수가 작아지므로, 정규화하면
"몸 크기의 몇 배만큼 움직였나"라는 카메라 거리·해상도 무관 기준이 된다.

기준값: 정지 0.0~0.2 / 보행 0.5~0.9 / 몸싸움 1.5 이상 → `ASSAULT_MOTION_THRESH = 1.2`

판정 구조도 "같은 track_id 쌍이 N초 연속"에서 **카메라 단위 점수 누적**으로 변경
(조건 만족 프레임마다 +1, 시간 경과 시 감쇠, 6점 초과 시 확정). track ID가 바뀌어도 증거가
사라지지 않아 ID churn에 영향받지 않는다.

### 02 기물파손 — MOG2 폐기, 기준 프레임 고정 방식

`createBackgroundSubtractorMOG2`는 "계속 보이는 것 = 배경"으로 학습하는 적응형 모델이라
**넘어진 진열대를 약 18프레임 만에 배경으로 흡수**해버려서 이벤트가 영원히 안 떴다(시뮬레이션으로 발견).
"바뀐 상태가 유지되는지" 판단에는 적응형 배경 모델이 원리적으로 맞지 않는다.

교체: 기준 프레임을 **정상 상태일 때만** 천천히 갱신하다가, 변화가 급증하면 갱신을 얼려 사건 직전
모습을 보존하고, 움직임이 잦아든 뒤 현재 화면과 비교해 차이가 유지되면 확정.
- 사람이 지나간 경우 → 지나가고 나면 차이 0 → 이벤트 없음
- 물건이 넘어진 경우 → 차이가 남음 → 3초 유지되면 02 확정

이후 임계 2건 추가 수정:
- **미탐**: 진열대 전도 순간의 화면 변화는 3.8%뿐이라 기존 6% 임계를 못 넘었음 → 트리거를
  "순간 움직임"에서 **"최근 2초간 변화량의 급상승(1.2%)"**으로 교체. 조명이 서서히 변하는 것과
  물건이 갑자기 넘어지는 것을 가르는 건 변화량이 아니라 **변화 속도**다.
- **오탐**: 모니터 영상의 장면 전환으로 화면 44%/60%가 한꺼번에 바뀌며 02 발생 → **35% 이상
  바뀌면 카메라 이동/장면 전환으로 간주**(`DAMAGE_MAX_DIFF_RATIO`).

### 06 화재 — 실기기 오탐 수정 (2026-09-14)

불이 없는데 `불꽃색 3.4%, 깜빡임 1.71`로 발생. 원인 2가지:
1. **화면 전체의 불꽃색 픽셀 수를 합산**했기 때문. 매장의 나무 테이블·베이지 상품·따뜻한 조명에
   흩어진 주황 픽셀이 다 더해져 임계를 넘었다. → **연결된 가장 큰 덩어리** 크기만 보도록 교체
   (`connectedComponentsWithStats`, 실제 불은 한 덩어리로 뭉쳐 있음).
2. flicker = 표준편차/평균 이라 **평균이 작으면 값이 폭증**한다. 임계에 아슬아슬하게 걸친 소량의
   픽셀이 자동노출 때문에 들락날락하면 flicker가 1.7까지 뛴다. 실제 불꽃은 0.2~0.6.
   → **flicker 상한(1.10) 추가**, 채도/명도 하한도 상향(S 100→130, V 150→190).

검증: 합성 화재 영상 감지 유지(덩어리 18.3%), 실제 매장 사진 + 자동노출 요동 재현 시 오탐 0
(후보 덩어리 0.00%).

## 중복 이벤트 억제 — 상황(incident) 단위 (2026-09-14)

**쿨다운 방식은 채택하지 않았다.** 60초 쿨다운이어도 쓰러진 사람이 3시간 방치되면 180건이 쌓이고
문자도 180번 나간다. 주기를 늘리면 이번엔 진짜 급한 상황을 늦게 알게 된다.

문제의 본질은 **쓰러짐·화재를 "순간 이벤트"로 다룬 것**이다. 실제로는 *시작되고 → 지속되다 →
끝나는 "상황"*이다. 그래서 `incident.py`에서 상황 단위로 묶는다:

```
최초 감지        → 전송 O   (CCTV_ISSUE 1건 + 알림 1회)
지속되는 동안     → 전송 X
일정 시간 미감지  → 상황 종료
다시 감지        → 새 상황으로 전송 O
```

**단, 묵살하면 안 된다** — 쓰러짐이 10분째라는 건 아무도 발견 못 했다는 뜻이라 더 위급하다.
그래서 정해진 시점에만 딱 한 번씩 **에스컬레이션**한다. 3시간 연속 쓰러짐 시뮬레이션 결과:

```
    0초  new          (최초)
  300초  escalation   (5분)
  900초  escalation   (15분)
총 3건 — 쿨다운 60초 방식이었다면 180건
```

코드별 정책(`incident.py`의 `CODE_POLICY`):

| 코드 | 상황종료 판정 | 재알림 시점 |
|---|---|---|
| 06 화재 | 60초 | 2분, 5분 |
| 01 폭행 | 90초 | 3분, 10분 |
| 03 쓰러짐 | 120초 | 5분, 15분 |
| 04 무단침입 | 180초 | 10분 |
| 02 기물파손 | 300초 | 없음 (이미 벌어진 일) |
| 05 장시간체류 | 600초 | 없음 |

**묶는 기준을 track_id가 아니라 code로 잡은 이유**: 낮은 FPS에서 ID가 바뀌면 같은 사람이 새 상황으로
잡혀 억제가 무력화된다.

전송 payload에 `stage`(`new`/`escalation`)와 `duration`(초)이 포함되므로, 알림 쪽에서 최초 발생과
장기 미조치 재알림을 구분해 처리할 수 있다. **알림 중복은 별도 작업 불필요** — NOTICEYN='N' 건을
폴링하는 구조라 INSERT가 줄면 문자도 자동으로 줄어든다.

## 손님 입·퇴장 (CCTV_VISITOR) — 2026-09-14

`CCTV_VISITOR` 컬럼: `NO / CNO / TRACK_ID / INTIME / OUTTIME / STAYTIME(분) / STATE / CDATE`
(STATE: 0=입장중, 1=정상퇴장, 2=장시간체류)

```
3초 이상 계속 보임 → 입장 INSERT (STATE=0, INTIME)
5초 이상 안 보임   → 퇴장 UPDATE (OUTTIME, STAYTIME, STATE=1 또는 2)
5분 이상 체류      → STATE=2
```

- **3초 조건을 둔 이유**: 화면 가장자리를 스쳐 지나가거나 한두 프레임 오탐된 것까지 손님으로
  집계되면 통계가 망가진다. 검증 완료(2초만 잡힌 사람은 집계 안 됨).
- **TRACK_ID 형식**: `20260914142208-C1-T7` — 워커 재시작 시 track_id가 1부터 다시 시작해
  충돌하는 것을 입장 시각을 붙여 방지.
- **워커 종료 시** 입장 중인 손님을 자동 퇴장 처리(`flush()`). 안 하면 STATE=0 행이 영원히 남아
  "현재 매장 인원"이 계속 늘어난 것처럼 보인다.

서버 엔드포인트(둘 다 재전송에 안전하도록 멱등하게 구현):
- `POST /api/cctv/visitor/enter` — 같은 trackId가 이미 있으면 중복 INSERT 안 함
- `POST /api/cctv/visitor/exit` — STATE=0인 행만 UPDATE

## 이슈 발생 위치 좌표 (호모그래피)

이슈가 매장 어디서 발생했는지를 도면 좌표로 보내는 기능. **X/Y는 CCTV_ISSUE가 아니라 AIISSUEMAP에
저장**되며(이은혜님 담당), 젯슨은 좌표를 만들어 보내는 역할만 한다.

**역할 분담**: 카메라 화면 좌표(`imgX/imgY/imgW/imgH`)는 젯슨만 알 수 있으므로 **설정 없이 항상
전송**한다. 도면 좌표(`x/y`)는 `homography.json`이 있을 때만 보내는 부가 정보다.
도면은 바뀔 수 있고(AI 도면 재생성 등) 그때마다 변환도 바뀌므로, 원칙적으로는 도면을 가진 서버
쪽에서 변환하는 게 유지보수에 유리하다.

**핵심 주의점**: 변환은 바닥 평면 위의 점에만 성립한다. 박스 **중심**을 쓰면 안 되고 반드시
**하단 중앙(발이 바닥에 닿는 점)**을 써야 한다. 검증 결과 박스 중심 사용 시 **0.92m 오차** 발생.

**좌표 단위**: 서버 `POST /api/shopmap/issue`는 xpos/ypos를 **0~1 비율로만** 받는다
(`if not 0 <= xpos <= 1: raise ValueError`). 도면 픽셀을 그대로 보내면 400. `homography.py`가
`plan_size`로 나눠 정규화해서 내보낸다. 도면 경계에 딱 걸친 점이 부동소수점 오차로 버려지지 않도록
2% 여유를 두고 경계로 당긴다.

**도면이 없을 때**: `make_plan.py`가 카메라 화면의 바닥을 위에서 본 모습으로 펴서(rectification)
도면을 만들어준다. 이때 펴는 데 쓴 네 점이 그대로 대응점이 되므로 `homography.json`도 같이 생성된다.
한계 — 완전 무클릭 자동 검출(`--auto`)은 진열대가 바닥을 가리면 대부분 실패하므로 네 점 직접 지정이
확실하다. 또 결과 도면은 바닥 사진을 편 것이라 높이가 있는 물체(진열대/의자)는 늘어져 보인다.

캘리브레이션 도구(웹페이지)로 카메라 이미지와 도면을 올리고 대응점을 클릭하면 JSON이 생성된다.

## 실행 방법

```
python3 worker_skeleton.py                     # CSI 카메라
python3 worker_skeleton.py --source test.mp4 --loop   # 테스트 영상 (권장)
python3 worker_skeleton.py --after-hours       # 04 즉시 테스트
python3 worker_skeleton.py --loiter 20         # 05 임계 20초로 낮춰 테스트
python3 worker_skeleton.py --no-report         # 서버 전송 없이 판정만
bash run_watch.sh [영상]                        # 디버그 화면 보며 튜닝
bash run_service.sh start|stop|log|status      # 백그라운드 상주
```

**영상 파일 입력을 권장**하는 이유: 모니터를 카메라로 찍으면 화질 때문에 YOLO 탐지부터 흔들려서
룰 문제인지 탐지 문제인지 구분이 안 된다. 영상 파일일 때는 `CAP_PROP_POS_MSEC`(영상 내부
타임스탬프)를 시간 기준으로 쓰므로 추론이 느려도 "3초 유지" 같은 조건이 영상 내용과 어긋나지 않는다.

**테스트 영상 생성기** `make_test_videos.py`: 실제 매장 CCTV 캡처를 배경으로 02(진열대 전도) /
06(불꽃) 검증용 mp4를 합성한다. 이 두 코드는 사람 탐지와 무관하므로 합성 영상으로도 실제와 동일한
경로를 검증할 수 있다. (01/03/05는 실제 사람이 필요해 합성 불가)

## 해결한 주요 이슈 기록

- **API 호출 시 버벅임** — `report_issue()`가 `requests.post()`를 **동기 호출**해서, 서버가 LLM으로
  comnet을 만드는 10초 이상 동안 카메라 메인 루프 전체가 정지하고 프레임이 유실됐다.
  → 생산자-소비자 패턴으로 분리(큐잉 측정값 0.0001초). GIL이 있어도 `requests`가 네트워크 응답을
  기다리는 동안 GIL을 놓기 때문에 메인 루프 추론은 정상 동작한다.
- **트래커 `KeyError`** — 매칭 루프 안에서 새 트랙을 `self.tracks`에 바로 추가해, 사람이 2명 이상일 때
  예측 위치에 없는 ID를 조회해 예외 발생. 읽는 대상(`prev_tracks`)과 쓰는 대상(`updated`)을 분리해
  해결. 같은 프레임의 두 사람이 한 트랙에 매칭되는 문제도 함께 해결됨.
- **트래커 매칭 거리** — 고정 픽셀값(180px)만 쓰면 첫 매칭 때 아직 속도를 몰라 빠르게 움직이는 사람이
  영영 같은 ID를 못 받는다. → **박스 크기에 비례**(폭의 2배)하게 변경. 짧은 미검출
  허용(`max_lost_sec=1.5초`)도 추가.
- yolov5 v5.0 `detect.py`의 `check_requirements()`가 시스템 opencv를 pip이 인식 못해 재설치를 시도,
  호환 안 되는 `typing_extensions`가 딸려와 실패 → 해당 호출 줄 주석 처리로 우회.
- H200 서버(`~/allimio`)는 foreground로 띄우면 SSH 끊길 때 같이 죽으므로 tmux 사용.

**회귀 테스트**: 시뮬레이션 5묶음 통과 — 정탐 8 / 오탐 7 / 트래커 5 / 호모그래피 10 / 중복억제·방문객 10.
오탐 케이스에 "나란히 걷는 두 사람", "붙어서 대화", "마주 걸어와 스쳐지나감", "사람이 지나감",
"조명 서서히 변화", "장면 전환" 포함.

## 확인/보완이 필요한 것

1. ~~**CCTV 스트림 주소 컬럼 없음**~~ — **[해결, 저장소 미반영]** `CCTV_STREAM` 테이블(1:1, CNO
   UNIQUE FK) 신설 + 관리자 화면 `dbms/cctvstream` 구현 완료. Jetson이
   `GET /cctv_stream/by_cno/{cno}`로 접속정보를 조회하고 `PUT /cctv_stream/update`로 연결상태를
   갱신하는 연동은 **미착수**.
2. ~~**CODE 참조 테이블 없음**~~ — **[해결]** `CCTV_ISSUE_CODE` 신설, 하드코딩 3곳 전부 제거
   (React `CODE_LABELS` → `useCctvIssueCodes` 훅 / `CctvIssueService.java` → `existsById` 검증 /
   FastAPI → `core/codes_cache.py`). **06(화재) DB 등록 완료(2026-09-13).**
   사이드바 노출은 `IN_MENU` 테이블에 하위 메뉴 직접 등록 필요(라우트는 동작).
3. ~~**기물파손(02) 미구현**~~ — **[해결]** 기준 프레임 고정 방식으로 구현.
4. **휴리스틱은 전부 정확도 낮은 MVP** — Jetson의 confidence가 곧 최종 RELIABILITY이므로 실측 튜닝이
   중요(서버가 걸러주지 않음). 정확도를 더 올려야 할 경우의 단계:
   (1) FPS 확보 — **완료(4→8fps)**
   (2) `trt_pose` 등 pose 추정으로 관절 좌표 기반 판정 — 학습 없이 가능, 나노에서 동작
   (3) 그래도 부족하면 AI-Hub 이상행동 CCTV 데이터셋으로 H200에서 action recognition 학습 후
       가중치만 젯슨으로 이전 (나노는 학습 불가, 추론만)
5. **알림 연동** — `CCTV_ISSUE.NOTICEYN`은 항상 'N'으로 생성. 이은혜님 담당 SMS/메일 발송 쪽에서
   미발송 건을 폴링해 발송 후 'Y'로 갱신하는 흐름으로 연결 필요. `stage` 필드로 최초/재알림 구분 가능.
6. **AIISSUEMAP 저장 미완성** — `POST /api/shopmap/issue`의 `create_issue_map()`은 LLM 분석 + 도면
   이미지 생성 + dict 반환까지만 하고 **DB INSERT가 없다**. Java의 `AiIssueMapService.create()`를
   호출하든 FastAPI가 직접 INSERT하든 연결 필요(이은혜님 담당 영역).
   또 요청 DTO가 `shopmapno`를 받는데 젯슨이 가진 건 `cno`라 **cno → shopmapno 매핑 합의 필요**.
7. **FastAPI cctv 모듈이 GitHub 미반영** — `cctv/router.py`, `cctv/service.py`, `cctv/schema.py`,
   `modules/cctv_issue.py`, `core/codes_cache.py` 5개 파일이 H200(`~/allimio`)에만 배포된 상태. 커밋 필요.
8. **`CNO=1` 하드코딩** → 실제 CCTV 번호로 교체 (1번 CCTV_STREAM 연동과 함께).
9. **01 폭행 임계 미세조정** — `ASSAULT_MOTION_THRESH=1.2`는 blob 근사로 측정한 값 기반이라,
   실제 YOLO 박스 기준 실측 후 재조정 필요. 디버그 화면의 `mot=` 값을 보며 조정.
10. **원격 관제 화면** — 현재 MJPEG 스트림은 디버그/튜닝용(젯슨 로컬). 관리자 대시보드에서 원격으로
    보려면 서버 경유 재배포 구조 별도 설계 필요(`CCTV_STREAM.STREAM_URL` 재사용 가능).